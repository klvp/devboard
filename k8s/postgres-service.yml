apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: devboard
spec:
  clusterIP: None
  selector:
    app: devboard-postgres
  ports:
    - protocol: TCP
      port: 5432 # host port on which user hit for service discovery
      targetPort: 5432 #pod or application running port
