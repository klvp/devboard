apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devboard
spec:
  selector:
    app: devboard-backend
  ports:
    - protocol: TCP
      port: 8080 # host port on which user hit for service discovery
      targetPort: 8080 #pod or application running port
